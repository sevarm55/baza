<?xml version="1.0" encoding="utf-8"?>
<!--
  По умолчанию только HTTPS и системные корни: своих сертификатов
  продукт не подкладывает и пиннинга не делает — сервер живёт за
  обычным публичным сертификатом, а пиннинг здесь означал бы мёртвое
  приложение в день его смены.

  Исключение ровно одно и только для отладочной сборки: локальный
  сервер разработчика. Без него отладочная сборка могла бы ходить
  только в прод, то есть по живым клиентам.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
