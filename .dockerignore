node_modules
.next
.git
.data
.env*
*.tsbuildinfo
prototype
TESTING.md
README.md

# Клиенты магазинов. Серверу они не нужны ни при сборке, ни при работе:
# образ собирает Next, а не Xcode и не Gradle. Без этих строк оба каталога
# едут в контекст сборки целиком — шрифты, снимки, обёртка Gradle, — и
# каждый выкат тащит на сервер десятки мегабайт, которые там не откроет
# никто.
ios
android
