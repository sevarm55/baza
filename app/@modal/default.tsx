/**
 * Пустое состояние слота: на всех адресах, кроме перехваченных,
 * модального окна нет. В Next 16 такой файл обязателен для каждого
 * параллельного слота — без него сборка падает.
 */
export default function Default() {
  return null;
}
