# Тест-кейсы «Tetr»

Ручная проверка в браузере. Если что-то не сходится — назови номер кейса,
что ожидал и что увидел.

## Сначала — то, что проверяет машина

Два прогона закрывают всё, что можно проверить без глаз. Гонять руками
то, что они и так проверяют, — потеря времени.

```bash
npm run smoke
```

Слой данных на чистой базе: снимки цен, атомарность записи, изоляция
бизнесов, расчёт зарплат, пробный срок, филиалы. Ничего не поднимает,
идёт секунд десять.

```bash
npm run db:up && npm run dev
```
```bash
npm run e2e
```

Сквозной прогон по живому серверу: регистрация, вход, токены, смена,
запись машины, отмена, расходы, зарплаты, выгрузка, роли, чужие правки,
истёкший срок, блокировка, доступность страниц. 95 проверок.

Порт можно любой — свой компьютер узнаётся по хосту, а не по номеру:

```bash
BASE=http://localhost:3211 npm run e2e
```

По проду — только чтение, ничего не пишет:

```bash
BASE=https://tetrin.pro npm run e2e
```

**Оба должны заканчиваться словами «все проверки пройдены».** Если нет —
дальше руками идти незачем, сначала чиним.

---

## Подготовка к ручной проверке

```bash
npm run db:up
```
```bash
npm run db:fresh
```
```bash
npm run dev -- --port 3100
```

Открыть http://localhost:3100

`db:fresh` сносит базу, накатывает миграции заново и заводит демо. Он же
печатает телефоны и коды — берите их оттуда, а не отсюда: демо меняется.

| Кто | Телефон | PIN | Роль |
|---|---|---|---|
| владелец | `+374 99 000 000` | `2468` | владелец |
| мойщик | `+374 99 000 001` | `1357` | мойщик, 40% |

---

## A. Экран сотрудника — главное

Войти как **Աշոտ** (`077 333 444` / `5678`).

**A1. Запись за три касания**
Нажать `+ մեքենա` → ввести `55 KL 210` → `Հաջորդ` → выбрать `Թափք` → `Կանխիկ`.
Ожидается: галочка «Գրանցված է», через секунду возврат на главную.

**A2. Счётчик обновился сам**
Сразу после A1 посмотреть на верхний блок.
Ожидается: число машин выросло на 1, выручка +3 000 ֏, заработок +1 200 ֏ (40%).
Страницу перезагружать не нужно — цифра должна обновиться сама.

**A3. Постоянный клиент узнаётся**
`+ մեքենա` → начать вводить `07 XY 991`.
Ожидается: под полем зелёная плашка «Արդեն եղել է 1 անգամ · վերջինը այսօր · ընդամենը 12 000 ֏».
Подсказка появляется во время набора, примерно через четверть секунды после последней буквы.

**A4. Регистр не создаёт двойников**
`+ մեքենա` → ввести `07 xy 991` маленькими буквами.
Ожидается: та же зелёная плашка, что и в A3. Это тот же клиент, а не новый.

**A5. Нельзя пойти дальше с пустым номером**
`+ մեքենա` → ничего не вводить.
Ожидается: кнопка `Հաջորդ` серая и не нажимается.

**A6. Enter вместо кнопки**
`+ մեքենա` → ввести номер → нажать Enter на клавиатуре.
Ожидается: переход к выбору услуги. Мойщику так быстрее.

**A7. Возврат назад не теряет введённое**
Дойти до оплаты → `Հետ` → `Հետ`.
Ожидается: номер машины на месте, ничего вводить заново не нужно.

**A8. Отмена сбрасывает всё**
`+ մեքենա` → ввести номер → `Չեղարկել` → снова `+ մեքենա`.
Ожидается: поле пустое.

**A9. Сотрудник не видит чужого**
Посмотреть на верхнюю панель.
Ожидается: нет переключателя «Սեփականատեր», нет вкладок владельца. Только своя смена.

**A10. Сотрудник не может зайти в кабинет владельца**
Ввести в адресной строке `localhost:3000/owner`.
Ожидается: выкидывает обратно на `/work`.

---

## B. Кабинет владельца

Выйти (иконка ⏻ справа вверху) и войти как **Արամ** (`077 111 222` / `1234`).

**B1. Цифры сходятся с экраном сотрудника**
Вкладка `Այսօր`.
Ожидается: выручка = сумма всех записей за сегодня, включая те, что сделал Աշոտ.
Проверить руками: 5 000 + 12 000 + всё, что добавил в разделе A.

**B2. Выработка по каждому**
Блок `Հերթափոխին`.
Ожидается: две строки — Արամ и Աշոտ, у каждого своё число машин и своя сумма.
Справа мелким: сколько ему причитается. У Արամ 0 ֏, потому что у владельца 0%.

**B3. Лента показывает, кто и что мыл**
Блок `Հոսք`.
Ожидается: каждая строка — номер машины, услуга, имя сотрудника, способ оплаты, время.

**B4. Наличные отдельно от карты**
Сделать запись с оплатой `Քարտ` (переключиться на вкладку `Աշխատակից` вверху → `+ մեքենա`).
Вернуться в `Այսօր`.
Ожидается: общая выручка выросла, а плитка `Կանխիկով` — нет.

**B5. Зарплаты**
Вкладка `Աշխատավարձեր`.
Ожидается: по каждому сотруднику — сколько машин, выручка, его процент и сумма к выплате.
Внизу пояснение про калькулятор.

**B5a. Расчёт с сотрудником**
Там же нажать `Նշել վճարվածը` у Աշոտ.
Ожидается: он исчез из списка к выплате, внизу появилась `Վճարումների պատմություն`
с суммой и датой.

**B5b. Двойная выплата невозможна**
Сразу обновить страницу.
Ожидается: долг у него по-прежнему 0, кнопки нет. Считается не «за неделю»,
а с момента последнего расчёта — поэтому дважды заплатить за одно и то же нельзя.

**B5c. Новая работа копится заново**
Переключиться на `Աշխատակից`, сделать запись, вернуться в `Աշխատավարձեր`.
Ожидается: в долге только эта новая запись, старое не всплыло.

**B5d. Период в дашборде**
Вкладка `Այսօր` → переключатели `Այսօր` / `7 օր` / `30 օր`.
Ожидается: цифры и лента меняются под выбранный период.

**B5e. Отмена ошибочной записи**
В ленте нажать `✕` на любой строке → подтвердить.
Ожидается: запись пропала, выручка уменьшилась на её сумму.
Проверить во вкладке `Հաճախորդներ`: у этого клиента визитов стало меньше.

**B5f. Сотрудник исправляет свою ошибку сам**
Войти как Աշոտ → в блоке `Վերջինները` нажать `✕` на своей записи.
Ожидается: запись отменена, счётчик заработка сразу уменьшился.
Чужие записи сотруднику не показываются вовсе — отменить их он не может.

**B6. База клиентов**
Вкладка `Հաճախորդներ`.
Ожидается: список номеров машин, у каждого число визитов и общая сумма.
`07 XY 991` должен показывать больше одного визита, если делал A3/A4 с записью.

**B7. Владелец может работать сам**
Переключатель вверху `Աշխատակից` / `Սեփականատեր`.
Ожидается: переключается между своим кабинетом и экраном записи. На маленькой мойке
владелец и сам моет — это его рабочий режим.

**B8. Добавление сотрудника**
Вкладка `Աշխատակիցներ` → заполнить: имя `Տիգրան`, телефон `077 555 666`, PIN `1111`, 35%.
Ожидается: появился в списке сверху, форма очистилась.

**B9. Новый сотрудник реально может войти**
Выйти → войти как `077 555 666` / `1111`.
Ожидается: попадает на экран записи, в счётчике «քո 35%».

**B10. Телефон нельзя занять дважды**
Войти владельцем → `Աշխատակիցներ` → добавить сотрудника с телефоном `077 333 444`.
Ожидается: красная плашка «Այս համարն արդեն գրանցված է», сотрудник не создан.

**B11. Неверный процент отклоняется**
Там же попробовать процент `150`.
Ожидается: браузер не даст отправить либо придёт ошибка про 0–100.

---

## B2. Настройки

**B12. Цена меняется, история — нет**
Вкладка `Կարգավորումներ` → у `Կոմպլեքս` поставить цену `6500` → `✓`.
Перейти в `Այսօր`.
Ожидается: старые записи с этой услугой по-прежнему показывают старую цену,
выручка за день не изменилась ни на драм.

**B13. Новая запись берёт новую цену**
Вкладка `Աշխատակից` → `+ մեքենա` → выбрать `Կոմպլեքս`.
Ожидается: в списке услуг стоит `6 500 ֏`. Записать и убедиться,
что в ленте рядом лежат две одинаковые услуги с разными ценами.

**B14. Новая услуга**
`Կարգավորումներ` → блок `Նոր ծառայություն` → название и цена → `+`.
Ожидается: появилась в списке и сразу доступна сотруднику при записи.

**B15. Убрать услугу**
У любой услуги нажать `✕`.
Ожидается: исчезла из выбора у сотрудника, но прошлые записи с ней
остались в ленте и в выручке. Она не удалена, а убрана из прайса.

**B16. Процент сотрудника**
`Աշխատակիցներ` → сменить процент у Աշոտ на `45` → `✓`.
Ожидается: сохранилось. При этом в `Աշխատավարձեր` уже посчитанные суммы
не поменялись — новый процент действует только на новые записи.

**B17. Владелец не может отключить сам себя**
`Աշխատակիցներ` → посмотреть на строку владельца.
Ожидается: у неё нет кнопки `✕`. Иначе он потеряет доступ к собственному кабинету.

**B18. Переименование бизнеса**
`Կարգավորումներ` → блок `Բիզնես` → сменить название → `Պահպանել`.
Ожидается: новое название в шапке на всех экранах.

**B19. Клиент-призрак не показывается**
Отменить единственную запись какого-нибудь клиента. Затем начать вводить его номер
при новой записи.
Ожидается: зелёной подсказки НЕТ. Клиент с нулём визитов не должен всплывать
с текстом «был 0 раз на 0 ֏». Если он приедет снова — снова появится в базе.

---

## B3. Абонементы

Главное здесь — деньги не должны посчитаться дважды.

**B20. Продажа**
Вкладка `Բաժանորդագրություններ` → номер машины, услуга, количество `10`, цена `8000`, срок `30`.
Под формой видно `10 × услуга = полная цена` и процент скидки.
Ожидается: абонемент появился в списке с остатком `10 -ից 10`, форма очистилась.

**B21. Продажа — это приход денег**
Вкладка `Այսօր`.
Ожидается: выручка выросла ровно на 8 000, появилась плитка `Բաժանորդագրություններից`.
Число машин НЕ выросло — продажа абонемента это не помытая машина.

**B22. Списание видно сотруднику**
`Աշխատակից` → `+ մեքենա` → тот же номер → выбрать ту же услугу.
Ожидается: первым в списке оплаты стоит зелёный `🎟 Բաժանորդագրություն` с остатком.
Он выделен намеренно: если у клиента абонемент, брать с него деньги повторно — прямая ошибка.

**B23. Деньги не считаются дважды**
Списать одну мойку и вернуться в `Այսօր`.
Ожидается: в ленте запись с ценой = номинал одной мойки (8 000 ÷ 10 = 800),
**но общая выручка НЕ выросла**. Деньги пришли при продаже.
Число машин выросло на 1, появилась плитка со счётчиком списаний.

**B24. Мойщик получает своё**
`Աշխատավարձեր`.
Ожидается: у мойщика, который мыл по абонементу, долг вырос на его процент от 800.
Выручки не было, но машину он помыл.

**B25. Средний чек не раздувается**
Там же в `Այսօր` посмотреть `Միջին չեկ`.
Ожидается: считается только по оплаченным на месте. Иначе один проданный
абонемент на 40 000 сделал бы средний чек бессмысленным.

**B26. Отмена возвращает мойку**
Отменить абонементную запись крестиком → вернуться в `Բաժանորդագրություններ`.
Ожидается: остаток снова `10 -ից 10`.

**B27. Исчерпание**
Продать абонемент на `1` мойку, списать её, потом попробовать списать ещё раз.
Ожидается: после первого списания вариант `🎟` у этого клиента больше не предлагается.

**B28. Чужой абонемент**
Ввести номер ДРУГОЙ машины и выбрать ту же услугу.
Ожидается: варианта `🎟` нет. Абонемент привязан к конкретному клиенту.

---

## C. Главное архитектурное обещание

**C1. Другая ниша — тот же код**
Выйти → `Ստեղծել` → выбрать `Ատամնաբուժարան` → телефон `077 777 000`, PIN `2222`.
Ожидается:
- поле клиента стало `Հիվանդի հեռախոս` вместо номера машины,
- услуги — Զննում, Մաքրում, Պլոմբ, Հեռացում, Իմպլանտ,
- единица учёта `ընդունելություն`, а не `մեքենա`,
- сотрудник называется `Բժիշկ`.

Это и есть «одно ядро, разные ниши»: ни строчки кода не поменялось.

**C2. Бизнесы не видят друг друга**
Находясь в стоматологии, посмотреть `Այսօր`.
Ожидается: выручка 0 ֏, лента пустая. Денег автомойки здесь нет и быть не должно.

**C3. Клиенты тоже изолированы**
В стоматологии сделать запись и начать вводить `12 AB 345`.
Ожидается: зелёной подсказки НЕТ. Этот клиент принадлежит другому бизнесу.

---

## D. Доступ и безопасность

**D1. Гость никуда не попадает**
Выйти. Ввести в адресной строке `localhost:3000/work`, потом `localhost:3000/owner`.
Ожидается: обоих случаях перекидывает на `/login`.

**D2. Неверный PIN**
На входе ввести `077 111 222` и PIN `9999`.
Ожидается: «Սխալ հեռախոս կամ PIN».

**D3. Несуществующий телефон**
Ввести `077 000 000` и любой PIN.
Ожидается: **та же самая** ошибка, что в D2. Разные тексты подсказали бы,
какие номера зарегистрированы.

**D4. Телефон в любом формате**
Войти владельцем, введя телефон как `+374 77 111 222`, потом как `77111222`.
Ожидается: оба варианта работают — это один и тот же номер.

**D5. Сессия переживает перезагрузку**
Войти → закрыть вкладку → открыть `localhost:3000` заново.
Ожидается: сразу свой экран, логиниться повторно не нужно.

---

## D2. Работа без связи

Главное для реальной мойки: она часто в подвале или за городом.

**D6. Запись без интернета**
Открыть DevTools (F12) → вкладка Network → поставить `Offline`.
Записать машину как обычно.
Ожидается: та же галочка «Գրանցված է». Никакой ошибки.
На главной появилась жёлтая плашка «1 գրանցում պահված է հեռախոսում…»,
а в списке запись помечена `Սպասում է կապի` и без крестика отмены.

**D7. Досылка при возврате связи**
Снять `Offline`.
Ожидается: запись уходит сама в течение секунды. Плашка исчезает,
счётчик смены вырастает на её сумму, запись становится обычной.
Обновлять страницу не нужно.

**D8. Двойная отправка не создаёт дубль**
Записать 2–3 машины офлайн, потом включить сеть.
Ожидается: в ленте у владельца ровно столько записей, сколько сделано.
У каждой записи свой идентификатор с телефона — повторная досылка
распознаётся как та же самая машина.

**D9. Приложение открывается офлайн**
Загрузить `/work` с сетью, затем поставить `Offline` и нажать F5.
Ожидается: экран открывается (из кэша service worker), кнопка записи работает.
Цифры смены будут на момент последней связи — это нормально.

**D10. Установка на телефон**
Открыть на телефоне → меню браузера → «Добавить на главный экран».
Ожидается: иконка с тремя столбиками, приложение открывается без адресной строки.

**D11. Выгрузка в Excel**
`Կարգավորումներ` → `Ներբեռնել 30 օրվա տվյալները (Excel)`.
Ожидается: скачался CSV, открывается в Excel без кракозябр,
колонки разложены правильно, отменённые записи помечены.

---

## F. Подписка

Состояние меняется скриптами — сервер при этом должен быть остановлен,
локальная база не пускает к себе два процесса сразу.

**F1. Триал виден владельцу**
Свежий бизнес, вкладка `Այսօր`.
Ожидается: жёлтая плашка `Փորձնական ժամկետից մնացել է 6 օր`
(срок берётся из `lib/plan.ts`).

**F2. Сотрудник плашку не видит**
Войти сотрудником.
Ожидается: никаких сообщений про оплату. Это забота владельца, а не мойщика.

**F3. Срок вышел — кабинет закрыт целиком**
```bash
npm run trial -- +37499000000 -1
```
Открыть `Այսօր`.
Ожидается: уводит на стену `Ժամկետը լրացել է`. Не «часть работает, часть
нет»: мягкая блокировка читалась невнятно — продукт говорил «срок вышел»
и при этом пускал ходить по разделам.

**F4. Но свои данные забрать можно всегда**
В том же состоянии — выгрузка в Excel и удаление аккаунта.
Ожидается: работают. Отбирать у человека его собственные цифры за
неуплату нельзя, чем бы это ни было обосновано.

**F4a. Мойщика закрывает тоже**
В том же состоянии войти мойщиком.
Ожидается: он тоже не работает. Это не наказание работнику, а прямое
следствие: мойка не оплачена, значит мойки в продукте нет.

**F5. Оплата снимает блок**
```bash
npm run activate -- +37499000000 3
```
Ожидается: плашка исчезла, запись снова работает.

**F6. Предупреждение перед концом**
```bash
npm run activate -- +37477111222 0
```
(не сработает — минимум 1 месяц; вместо этого `npm run trial -- <телефон> 3`)
Ожидается: плашка появляется за 5 дней до конца, а не постоянно.
Вечная плашка перестаёт читаться.

---

## E. Телефон

Экран сотрудника живёт на телефоне, поэтому это не мелочь.

**E1. Узкий экран**
Сузить окно браузера до ширины телефона (или F12 → режим устройства, iPhone).
Ожидается: ничего не уезжает за край, кнопки крупные, попадать пальцем удобно.

**E2. Поле не зумит страницу**
На узком экране тапнуть по полю ввода номера.
Ожидается: страница не «прыгает» и не увеличивается.

**E3. Сумма не разрывается**
Посмотреть на суммы вида `12 000 ֏` при узком экране.
Ожидается: число не переносится на две строки посередине.

---

## G. Филиалы

Один человек, две мойки, один номер телефона. Платит за каждую отдельно.

**G1. Вторая точка заводится на сайте**
Владелец → `Իմ մասնաճյուղերը` → завести вторую.
Ожидается: создалась. Пробного срока у неё НЕТ — она сразу ждёт оплаты.
Пробный даётся человеку один раз, а не каждой его мойке.

**G2. И это видно словами, а не «срок истёк»**
Открыть вторую точку.
Ожидается: `Սպասում է վճարման`, а не `Ժամկետը լրացել է`. Разница важная:
человек ничего не просрочил, он просто ещё не платил.

**G3. Вход ведёт в работающую точку**
Выйти, войти тем же номером и кодом.
Ожидается: попал в ОПЛАЧЕННУЮ точку, а не в закрытую. Владельца с
неоплаченной второй мойкой нельзя высаживать на стену при работающей
первой — тем более в приложении, где переключателя точек может не быть.

**G4. Переключение**
Боковая колонка → переключатель точек → вторая.
Ожидается: перешёл. Цифры, люди и услуги — второй точки, не первой.

**G5. Цифры не смешиваются**
Записать машину на первой точке, перейти на вторую.
Ожидается: на второй её нет. Совсем.

**G6. Уволенный на одной работает на другой**
Нанять человека на обе точки, уволить на первой.
Ожидается: на второй он работает как раньше, со своим процентом.

**G7. Удаление точки не убивает человека**
Удалить первую точку.
Ожидается: владелец входит тем же номером и попадает во вторую.
Пробный срок при этом остаётся израсходованным — иначе его можно было
бы получать заново бесконечно, удаляя и заводя мойку.

**G8. В приложении раздел точек появляется только со второй**
Ожидается: у кого мойка одна — раздела нет вовсе. Рассказывать ему про
устройство, которого он не просил, незачем.

**G9. Завести точку из приложения нельзя**
Ожидается: на странице точек написано, что новая заводится на сайте.
Это не недоделка: правила App Store (3.1.3f) не разрешают начинать
платный путь внутри приложения, а вторая точка платная сразу.

---

## Перед продажей — короткий список

Если времени мало, эти двенадцать. Каждый из них — то, после чего
клиент уходит и не возвращается.

1. `npm run smoke` и `npm run e2e` — оба зелёные.
2. **B1** цифры владельца сходятся с экраном мойщика до драма.
3. **B5a/B5b** зарплата считается и не выплачивается дважды.
4. **B12** смена цены не переписывает прошлые записи.
5. **A9/A10** мойщик не видит чужого и не входит в кабинет.
6. **C2** два бизнеса не видят друг друга.
7. **D6/D7** запись без связи не теряется и доезжает.
8. **D11** выгрузка в Excel открывается и в ней те же числа.
9. **F3/F4** просрочка закрывает вход, но выгрузка работает.
10. **G3** вход с двумя точками ведёт в работающую.
11. **E1** на своём телефоне, а не в эмуляторе: ничего не уезжает за край.
12. Армянский — прочитать вслух носителю. См. ниже.

---

## Чего пока НЕТ — это не баги

Чтобы не тратить время на уже известное:

- **Продажи абонемента сотрудником** — пока продаёт только владелец
- **Приёма платежей картой** — оплата отмечается скриптом `npm run activate`
- **Контактов для продления** — в тексте блокировки нет телефона, его надо вписать
- **Офлайн-отмены** — записи в очереди отменить нельзя, только после отправки

## Отдельно: армянский текст

Все надписи писал я, носителем не проверял. Если что-то звучит криво или
неестественно — это самое ценное, что можно найти на этом этапе.
Особенно проверить: `Պետհամարանիշ`, `Լվացող`, `Թափք`, `Սալոն`, `Հերթափոխ`,
`Հասույթ`, `Վճարման ենթակա`.
