/**
 * Малый загрузчик: та же волна, три детали.
 *
 * Живёт внутри кнопок и строк. Фирменный морф сюда не ставится
 * сознательно: кнопку «записать» жмут сорок раз за смену, и фигура,
 * которая на каждое нажатие собирается в квадрат, через неделю
 * начинает раздражать. Праздник — на запуске, в работе достаточно
 * признака жизни.
 *
 * Размер задан в `em` и берётся от кегля кнопки: индикатор внутри
 * крупной кнопки мойщика обязан быть крупнее, чем внутри строчной
 * кнопки в списке, и следить за этим вручную в двадцати местах некому.
 *
 * Целиком на CSS, без анимационного узла: этот индикатор попадает в
 * каждую форму продукта, и платить за него разметкой в каждой из них
 * незачем.
 */
export function TetrinMiniLoader({ className = '' }: { className?: string }) {
  return (
    <span className={`tetrin-mini ${className}`} aria-hidden>
      <i />
      <i />
      <i />
    </span>
  );
}
