-- Первый запуск: сценарий вместо чек-листа.
--
-- Одна колонка на участии владельца — где человек остановился в
-- сценарии первого запуска: услуги → расход → работник → смена глазами
-- работника → первая машина → готово. Это указатель ПОЗИЦИИ в сценарии,
-- а не выполненность шагов: сами данные бизнеса по-прежнему первичны, и
-- при загрузке сценарий сверяется с ними (см. lib/first-run.ts) — шаг,
-- чьи данные уже есть, не показывается и дублей не создаёт.
--
-- NULL означает «сценарий неприменим»: все существующие участия, все
-- работники и вторые точки владельцев. Показывать работающей мойке
-- обучение первого дня значило бы объяснять человеку его же бизнес.
-- Новым владельцам значение проставляет регистрация (lib/tenant.ts).

ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "onboarding_stage" text;
