-- Телефон клиента: владелец вписывает его сам из карточки.
--
-- Только добавление колонки, ничего не переписывается и не удаляется:
-- миграция безопасна для работающего сервера, старый код о поле не
-- знает и продолжает работать как раньше.
ALTER TABLE "clients" ADD COLUMN IF NOT EXISTS "phone" text;
