/**
 * Свёрнута ли боковая колонка.
 *
 * Обычный модуль, а не часть клиентского компонента, и это не выбор
 * стиля. `RAIL_BOOT` подставляется в разметку сервером, а импорт
 * значения из файла с `'use client'` даёт серверу не строку, а ссылку
 * на клиентский модуль: в HTML уходила пустота, класс до отрисовки
 * никто не ставил, и свёрнутая колонка разворачивалась при каждой
 * перезагрузке. Ошибка тихая — ни сборка, ни типы её не видят.
 */

export const RAIL_KEY = 'tetr_rail';
export const RAIL_CLASS = 'rail-tight';

/** Ставит класс до первой отрисовки. Вставляется строкой в `<head>`. */
export const RAIL_BOOT = `try{if(localStorage.getItem('${RAIL_KEY}')==='1')document.documentElement.classList.add('${RAIL_CLASS}')}catch(e){}`;
