/**
 * То из одноразовых кодов, что нужно и браузеру тоже.
 *
 * Отдельный файл на одну константу выглядит избыточным ровно до первой
 * попытки взять её из `lib/otp.ts`: тот тянет за собой `node:crypto` и
 * базу, и импорт из клиентского компонента ломает сборку. Число цифр при
 * этом обязано быть одним и тем же по обе стороны — иначе форма примет
 * шесть, а сервер будет ждать другого.
 */
export const CODE_LENGTH = 6;
