/**
 * Условия продукта в одном месте.
 *
 * Раньше срок триала жил в трёх: в коде регистрации, в кнопке лендинга
 * и в подписи под ценой. Достаточно поправить два из трёх — и продукт
 * обещает одно, а делает другое.
 */

/** Сколько дней бесплатно после регистрации. */
export const TRIAL_DAYS = 6;

/** Цена подписки в минимальных единицах валюты (для AMD — драмы). */
export const PRICE = 15000;

/**
 * Приложение в App Store.
 *
 * Здесь, рядом с ценой и сроком, а не в переменной окружения. Пока
 * приложение ждало ревью, адреса не существовало вовсе, и витрина
 * показывала знак магазина без ссылки. Теперь адрес есть, он постоянен и
 * публичен — это такое же условие продукта, как цена: спрятанный в
 * окружение, он молча превратится в «скоро» на сервере, где переменную
 * забыли вписать.
 *
 * Идентификатор выдан магазином при публикации и с версиями не меняется.
 * Без кода страны намеренно: Apple сама уводит человека в его витрину, а
 * прибитый `/am/` показал бы армянский магазин тому, у кого учётная
 * запись другой страны.
 */
export const APP_STORE_URL = 'https://apps.apple.com/app/id6796829076';
