/**
 * Марка.
 *
 * Отдельным файлом, а не строкой в словаре, потому что переводу она не
 * подлежит ни на одном языке и никогда: Tetrin — имя, а не слово. Живёт
 * здесь ещё и затем, чтобы знак и название можно было нарисовать в
 * компоненте, который попадает и в серверное дерево, и в клиентское, —
 * такому компоненту словарь недоступен ни серверный, ни браузерный.
 */
export const BRAND = 'Tetrin';

/**
 * Телефон, по которому отвечает живой человек.
 *
 * Здесь, а не в словаре: номер не переводится, как и марка, а нужен он
 * сразу трём местам — витрине, странице поддержки и стене про оплату.
 * До этого он лежал вписанным в двух видах и в четырёх файлах, и
 * расходились бы они молча.
 *
 * Два вида, потому что нужны оба: `tel:` не терпит пробелов, а человек
 * не читает номер слитно.
 */
export const SUPPORT_PHONE = '+37499855546';

export const SUPPORT_PHONE_HUMAN = '+374 99 855 546';
